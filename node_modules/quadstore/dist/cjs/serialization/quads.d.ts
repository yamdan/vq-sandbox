import type { DataFactory } from 'rdf-js';
import type { Prefixes, Quad, TermName } from '../types';
export declare const quadWriter: {
    writtenValueLength: number;
    write(prefix: string, value: Uint16Array | undefined, baseValueOffset: number, quad: Quad, termNames: TermName[], prefixes: Prefixes): string;
};
export declare const quadReader: {
    subject: null;
    predicate: null;
    object: null;
    graph: null;
    read(key: string, keyOffset: number, value: Uint16Array, valueOffset: number, termNames: TermName[], factory: DataFactory, prefixes: Prefixes): Quad;
};
